<tr>
  <td><%= test.title %></td>
  <td><%= t(".#{test_level(test)}")  %></td>
  <td><%= test.questions.count %></td>
  <td><%= button_to  t('.button_name'), start_test_path(test), class: 'btn btn-success' %></td>
</tr>
