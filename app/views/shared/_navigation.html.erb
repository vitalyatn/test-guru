<p class="nav user">  <%= link_to 'TestGuru', root_path %> </p>
<% if user_signed_in? %>
  <p class="nav user">
    <%= t('.welcome', name: current_user.first_name) %><!--Welcome, <b><%= current_user.first_name %></b>  Guru <br> !-->
    <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
  </p>
<% else %>
  <p class="nav user">
    <%= link_to t('.sign_up'), new_user_registration_path %> |
    <%= link_to t('.sign_in'), new_user_session_path %>
  </p>
<% end %>
