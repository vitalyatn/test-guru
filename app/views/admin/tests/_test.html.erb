<tr>
  <td class="align-middle">
    <%= content_tag :span, test.title, class: 'test-title', data: {test_id: test.id } %>
    <%= render 'form_inline', test: test == @test ? @test : test %>
  </td>
  <td class="align-middle"><%= t("tests.test.#{test_level(test)}") %></td>
  <td class="align-middle"><%= test.questions.count %></td>
  <td class="align-middle">
    <%= "#{test.time_limit/60} мин." unless test.time_limit.zero?%>
    <%= "без ограничений" if test.time_limit.zero?%>
  </td>

  <td class="align-middle"><%= link_to t('.show'), admin_test_path(test) %></td>
  <td class="align-middle">
    <%= link_to t('.edit'),
                edit_admin_test_path(test),
                data: { test_id: test.id},
                class: 'form-inline-link' %>
  </td>
  <td class="align-middle">
     <%= link_to t('.delete'),
                  admin_test_path(test),
                  method: :delete,
                  data: {confirm: 'Are you sure?'}%>
  </td>
</tr>
