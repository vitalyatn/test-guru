<h1><%= t('.header') %></h1>
<p>
  <%= link_to t('.new'), new_admin_test_path %>
</p>

<table>
  <tr>
    <th><%= t('tests.index.title') %></th>
    <th><%= t('tests.index.level') %></th>
    <th><%= t('tests.index.count') %></th>
    <th colspan="3"><%= t('tests.index.actions') %></th>
  </tr>

  <%= render @tests %>

</table>
